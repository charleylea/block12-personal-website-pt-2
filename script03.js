function simulateButtonClick() {
  document.getElementById("submit").click(); // This will trigger the click event on the submit button
}

// Automatically trigger the button click after 5 seconds
setTimeout(simulateButtonClick, 5000);
